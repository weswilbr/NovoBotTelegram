# NOME DO ARQUIVO: requirements.txt
# REFACTOR: Lista as dependências do projeto para garantir um ambiente de execução consistente.

# Biblioteca principal para interagir com a API do Telegram e para agendamento de tarefas (jobs)
python-telegram-bot[job-queue]==21.9

# Adaptador para o banco de dados PostgreSQL (versão binária, mais fácil de instalar)
psycopg2-binary

# Biblioteca para a criação e manipulação de arquivos PDF (usada nos relatórios)
fpdf2

# Biblioteca para processamento de imagens (provavelmente usada na criação de artes)
Pillow

# Carrega variáveis de ambiente de um arquivo .env para a aplicação
python-dotenv==1.0.0